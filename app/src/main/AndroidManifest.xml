<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.github.nekdenis"
          android:versionCode="1"
          android:versionName="0.5">

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.github.nekdenis.activity.MainActivity"
            android:label="@string/app_name">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activity.ColorMatcherActivity"></activity>
        <activity android:name=".activity.ColorChooserActivty"></activity>
        <activity android:name=".activity.ResultActivity"></activity>
        <activity android:name=".activity.ColorConverterActivty"/>
        <activity android:name=".activity.UserInfoActivity"/>

    </application>

</manifest>
